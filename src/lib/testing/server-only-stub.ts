// Stub usado só pelo Vitest (ver vitest.config.ts alias). O pacote real
// "server-only" lança erro fora do bundler do Next.js — em teste isso é
// esperado/seguro, então substituímos por um módulo vazio.
export {};
